[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && echo '=== BOT STARTING ===' && python -u -m bot.main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
